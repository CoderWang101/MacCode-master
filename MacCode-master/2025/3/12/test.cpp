#include <iostream>
using namespace std;

int main() {
    // double handsome_p = 0.0;
    // double handsome_np = 0.0;
    // double nature_p = 0.0;
    // double nature_np = 0.0;
    // double goforword_p = 0.0;
    // double goforword_jp = 0.0;
    // double goforword_np = 0.0;

    // // 嫁
    // handsome_p = 4.0 / 5; // 嫁的情况下还帅
    // nature_p = 4.0 / 5;   // 嫁的情况下还是性格好
    // goforword_p = 1.0 / 5; // 嫁的情况下还是上进心不强
    // //goforword_jp = 1.0 / 5; // 嫁的情况下还是一般

    // // 不嫁
    // handsome_np = 1.0 / 5; // 不嫁的情况下还帅
    // nature_np = 3.0 / 5;   // 不嫁的情况下还是性格不好
    // goforword_jp = 3.0 / 5; // 不嫁的情况下还是上进一般
    // //goforword_np = 2.0 / 5; // 不嫁的情况下还是上进不上进

    // double p = 0.0;

    
    // p = 0.5*((1.0-handsome_p) * (1.0-nature_p) * goforword_p)+0.5*((1.0-handsome_p) * nature_np * goforword_jp);
    // cout << p << endl;

    short int n=32767;//16bit
    int x=(int)n;//32bit
    cin >> n;
    cout << n << endl;
    cout << x << endl;

    return 0;
}