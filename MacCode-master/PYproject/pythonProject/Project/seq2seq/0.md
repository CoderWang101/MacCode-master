## 1. preprocessing data ---数据id化和dataset生成 实现词典 Dataset Tokenizer word levelTokenizer 两本词典 DataLoader

## 2. build model

### 2.1 encoder构建（Embedding层，循环神经网络使用GRU）

### 2.2 attention构建 实现Bahdanau ----重点，难点 # 2.3 decoder构建 用的lstm变种GRU

### 2.4Sequence2Sequence

### 2.5 loss(去padding部分损失)& optimizer # 2.6 train 每次epoch调用train

## 3. evaluation （不适合看准确率） bleu指标

### 3.1 given sentence, return translated results

### 3.2 visualize results (attention) 可视化，作者解释为什么有效
